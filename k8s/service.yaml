apiVersion: v1
kind: Service
metadata:
  name: enhancex-service
  labels:
    app: enhancex
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    name: http
  - port: 50051
    targetPort: 50051
    name: grpc
  selector:
    app: enhancex
